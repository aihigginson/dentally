# Pinned for reproducible, auditable builds. Versions match the tested local env
# (pyodbc/gunicorn pinned to current stable — pyodbc is mocked in tests, gunicorn
# is prod-only). Bump deliberately and re-run `pip-audit -r requirements.txt`.
Flask==3.1.3
flask-cors==6.0.2
msal==1.36.0
requests==2.33.1
pyodbc==5.2.0
python-dotenv==1.2.2
gunicorn==26.0.0
PyJWT==2.13.0
cryptography==48.0.1
azure-identity==1.19.0
azure-keyvault-secrets==4.9.0
azure-communication-email==1.0.0
